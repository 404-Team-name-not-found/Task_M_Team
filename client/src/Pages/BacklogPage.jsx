import React from 'react';
import Backlog from '../components/Backlog';

const BacklogPage = () => {
    return (
        <div>
            <Backlog/>
        </div>
    );
};

export default BacklogPage;